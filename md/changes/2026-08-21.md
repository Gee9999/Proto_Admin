# Changes — 2026-08-21

## 01:50 SAST — Supabase order-count and operational hardening

- Summary: Replaced eight order-tab count queries with one security-invoker RPC
  plus a one-scan deployment fallback. Added the missing foreign-key indexes,
  scheduled bounded retention for analytics, journey events, and rate limits,
  optimized auth RLS evaluation, removed overlapping admin SELECT policies, and
  pinned the four WhatsApp helper functions to an empty search path.
- Files affected: `api/admin-orders.js`, `lib/order-tab-counts.mjs`,
  `migrations/20260820234523_optimize_order_tab_counts.sql`,
  `migrations/20260820234524_operational_retention_and_fk_indexes.sql`,
  `migrations/20260820234525_rls_and_function_hardening.sql`,
  `migrations/20260820234526_remove_duplicate_retention_index.sql`,
  `tests/order-tab-counts.test.js`, and
  `tests/database-hardening-migrations.test.js`.
- Systems affected: Proto Admin order list API and the shared production Supabase
  project. The customer portal code and Vercel deployment were not changed.
- Verification: The live RPC returned `all=36`, `new=0`, `handed=32`,
  `progress=2`, `sent=2`, `paid=0`, and `unpaid=36`; execute access is limited to
  `service_role`. Four FK indexes, pg_cron 1.6.4, one active daily retention job,
  split write policies, and four invoker functions with pinned search paths were
  confirmed live. Targeted tests passed 6/6, ESLint passed, and the production
  build passed. The full suite passed 736/737 tests; the sole failure is the
  pre-existing Image Processing Centre stale queue-refresh race, which also
  fails in isolation and is unrelated to these files. Supabase
  advisors no longer report the 4 mutable search paths, 4 unindexed foreign keys,
  4 auth RLS initplans, or 2 overlapping permissive policies. A duplicate index
  detected after migration was removed with the corrective migration.
- Deployment status: All database migrations applied to production. Application
  commit, push, Vercel deployment, and live API verification pending.
- Commit or PR: Pending.
